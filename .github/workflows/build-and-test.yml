
name: Build and test

on:
  pull_request:
    branches: [ master, main, cutcorners-master ]
  push:
    branches: [ master, main, cutcorners-master ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
